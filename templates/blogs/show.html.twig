{% extends "base.html.twig" %}

{% block title %}
  {{ parent() }}|{{ blog.title }}
{% endblock %}

{% block body %}
  Affichage du blog {{ blog.id }}

  <h1>{{ blog.title }}</h1>

  <a href="{{ url("blog_edit", {id: blog.id}) }}">Edit</a>
{% endblock %}